import React from "react";

const Dashboard = () => {
    return (
        <div className='dashboard-container' style={{ margin: "auto" }}>
            Dashboard
        </div>
    );
};

export default Dashboard;
